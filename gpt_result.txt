Below is what I assumed each module would contain based on typical **game-engine / ECS-style architecture** and the names used in your tree. I’ll describe the **responsibility**, **main structures**, and **typical functions** expected inside each file.

---

# AI

## `AI/behaviour_tree`

**Purpose:**
Implements a **Behaviour Tree system** used for complex AI decision making.

**Typical content**

**Structures**

* `BTNode`
* `BTComposite` (Sequence / Selector)
* `BTDecorator`
* `BTAction`
* `BehaviourTree`

**Node types**

* `Sequence`
* `Selector`
* `Parallel`
* `Condition`
* `Action`
* `Inverter`
* `Repeater`

**Typical functions**

```
bt_create()
bt_destroy()

bt_tick(tree, entity)

bt_sequence_tick(node)
bt_selector_tick(node)

bt_condition(...)
bt_action(...)
```

**Usage**

```
Enemy AI
NPC decision logic
Complex scripted behaviors
```

---

## `AI/state_machine`

**Purpose:**
Handles **Finite State Machines (FSM)** for simpler AI.

**Structures**

```
State
StateMachine
Transition
```

Example states

```
IDLE
PATROL
CHASE
ATTACK
FLEE
```

**Typical functions**

```
fsm_create()
fsm_destroy()

fsm_update(machine, entity)

fsm_change_state(machine, new_state)
fsm_add_transition(...)
```

**Usage**

```
Enemy states
NPC behaviors
Boss phases
```

---

# Engine

## `Engine/animation`

**Purpose:**
Handles **sprite animations**.

**Structures**

```
Animation
AnimationFrame
Animator
```

**Typical data**

```
frames[]
frame_time
loop
current_frame
elapsed_time
```

**Typical functions**

```
animation_create(...)
animation_update(...)
animation_reset(...)
animator_play(...)
animator_stop(...)
```

**Usage**

```
Character animations
UI animations
Effect animations
```

---

## `Engine/camera`

**Purpose:**
Handles the **game camera / view transformation**.

**Structures**

```
Camera
```

**Fields**

```
position
zoom
rotation
viewport
target
```

**Typical functions**

```
camera_create()
camera_update()
camera_move()
camera_follow(entity)
camera_world_to_screen()
camera_screen_to_world()
```

**Usage**

```
Player following
Map scrolling
Zoom
```

---

## `Engine/depth_sorter`

**Purpose:**
Sorts entities by **render depth** before drawing.

Used for **2.5D / isometric engines**.

**Structures**

```
DepthItem
DepthSorter
```

**Typical functions**

```
depth_sorter_add(entity)
depth_sorter_clear()
depth_sorter_sort()
depth_sorter_get_order()
```

Possible sort criteria

```
entity->position.y
entity->layer
entity->z
```

---

## `Engine/renderer`

**Purpose:**
Centralized **render pipeline**.

Responsible for sending draw commands to **CSFML**.

**Structures**

```
Renderer
RenderCommand
```

**Typical functions**

```
renderer_init()
renderer_shutdown()

renderer_begin_frame()
renderer_end_frame()

renderer_draw_sprite(...)
renderer_draw_text(...)
renderer_draw_shape(...)
```

**Responsibilities**

```
Batching
Layer ordering
Camera transforms
Debug draw
```

---

# Entities

## `Entities/player`

**Purpose:**
Player entity logic.

**Structure**

```
Player
```

Typical components

```
position
velocity
sprite
animator
state
health
```

**Typical functions**

```
player_create()
player_destroy()

player_update()
player_handle_input()

player_attack()
player_move()
```

**Responsibilities**

```
Player-specific behavior
Input translation
Ability handling
```

---

# Systems

These look like **ECS systems**.

---

## `Systems/collision_system`

**Purpose:**
Handles **physics collisions** between entities.

**Structures**

```
Collider
Collision
```

**Typical functions**

```
collision_system_update()

check_collision(a, b)
resolve_collision(a, b)

collision_aabb(...)
collision_circle(...)
```

Possible features

```
Tile collisions
Entity collisions
Triggers
```

---

## `Systems/interaction_system`

**Purpose:**
Handles **interactions between entities**.

Example interactions

```
NPC talk
Pickup items
Activate lever
Open door
```

**Typical functions**

```
interaction_system_update()

check_interaction(player, entity)
trigger_interaction(entity)
```

---

## `Systems/movement_system`

**Purpose:**
Updates entity **movement**.

**Typical functions**

```
movement_system_update()

apply_velocity(entity)
apply_acceleration(entity)

movement_apply_input(entity)
```

Example

```
position += velocity * dt
```

---

## `Systems/vision_system`

**Purpose:**
Handles **AI perception / line-of-sight**.

**Structures**

```
VisionComponent
```

Fields

```
view_distance
view_angle
last_seen_position
```

**Typical functions**

```
vision_system_update()

can_see(observer, target)
compute_fov(...)
raycast_visibility(...)
```

Used for

```
Enemy detection
Stealth mechanics
```

---

# UI

---

## `UI/button`

**Purpose:**
Clickable UI button component.

**Structure**

```
UIButton
```

Fields

```
position
size
text
state
callback
```

States

```
NORMAL
HOVER
PRESSED
DISABLED
```

**Typical functions**

```
button_create()
button_update()
button_draw()

button_set_callback(...)
button_is_clicked(...)
```

---

## `UI/hud`

**Purpose:**
Heads-Up Display.

Displays gameplay info.

Examples

```
Health
Ammo
Score
Minimap
```

**Structure**

```
HUD
```

**Typical functions**

```
hud_create()
hud_update()
hud_draw()

hud_set_health(...)
hud_set_score(...)
```

---

## `UI/panel`

**Purpose:**
Generic **UI container**.

Panels group multiple UI elements.

**Structure**

```
UIPanel
```

Fields

```
position
size
children[]
visible
```

**Typical functions**

```
panel_create()
panel_add_element()
panel_remove_element()

panel_draw()
panel_update()
```

Used for

```
menus
inventory
dialog windows
```

---

## `UI/ui_manager`

**Purpose:**
Top-level **UI system controller**.

Manages **all UI elements**.

**Structure**

```
UIManager
```

Responsibilities

```
UI update loop
UI rendering
Event dispatching
Focus management
```

**Typical functions**

```
ui_manager_init()
ui_manager_shutdown()

ui_manager_update()
ui_manager_draw()

ui_manager_add_element(...)
ui_manager_remove_element(...)
ui_manager_handle_event(...)
```

---

# Architectural idea I assumed

Your tree looked like a **hybrid ECS architecture**:

```
Entities
    hold components

Systems
    operate on components

Engine
    low-level services

AI
    decision logic

UI
    interface layer
```

Example runtime loop:

```
input
AI
movement_system
collision_system
interaction_system
vision_system
animation
depth_sort
renderer
UI
```

---

If you want, I can also explain **what I expected the missing modules to be**, which might help you detect **architecture inconsistencies in BSCP**.
